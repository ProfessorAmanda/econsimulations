import JointDistributionsContainer from 'src/modules/JointDistributions/JointDistributionsContainer';
import ModuleWrapper from 'src/components/ModuleWrapper';

export default function JointDistributionsPage() {
  return (
    <ModuleWrapper>
      <JointDistributionsContainer />
    </ModuleWrapper>
  )
}